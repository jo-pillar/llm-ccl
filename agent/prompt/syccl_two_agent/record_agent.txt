You are the record agent for LLM-CCL. Write the historical record entry for the latest sketch attempt.

Return exactly one record for this attempt. Output only one JSON object, without markdown fences or surrounding text.

Use this exact JSON schema:
{{
  "summary": "Trail id: {trail_id}\n- transmissions strategy: <brief strategy inferred from the sketch and profile>\n- compact dsl: <the compact SketchDSL for this attempt>\n- profile data: <validity, completion time, expanded events, sketch bottleneck profile, critical transmission/chain, stage pressure, best_so_far>",
  "direction_hint": "<optional concise guidance for the next proposal>"
}}

Preserve useful failures, bottlenecks, and best structures. Keep each field concise.

Proposal LLM output:
{proposal_output}

{payload_json}
